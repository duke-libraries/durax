<%= f.input :curator_email, as: :email, required: f.object.required?(key),
  input_html: { class: 'form-control', multiple: false }
%>

<%= f.input :curator_email, as: :text, input_html: { rows: '14', mulitple: false}, 
	required: f.object.required?(key)
%>